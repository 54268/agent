# Invalid candidate comparison

This first diagnostic assigned a different optimiser seed to each B2
candidate.  It is retained for audit only and must not be used for model
selection or scientific claims.  The corrected screen is stored alongside it
under `b2_candidate_screen_fair/`, where every candidate shares the same seed
within each held-out inner fold.
