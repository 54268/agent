# Stage-6 诊断运行状态

Stage-6 已停止调参，代码作为只读历史基线保留。

`stage6_screen_v2` 至 `stage6_screen_v7` 均是不同修正版本的单 fold 诊断运行，部分目录还可能缺少完整候选、五折聚合或 `stage6_lco_selection.json`。它们不是完成的 LCO 报告，不能用于选模、跨数据集结论或论文主表。

## 可保留的诊断结论

- 修正公平 B2 后，通用 latent 消息没有产生稳定 H-score 净增益。
- V7 的最佳通信对比仅约 `ΔH=+0.0018`，同时查询率约 `0.3%`，属于门控塌缩而不是有意义的协作。
- V1 的较大增益依赖过弱的无通信对照，不作有效多智能体证据。
- 后续工作转入 Stage-7 的“本地互补性 → 强制条件消息 → 按需调度”逐级闸门。

原始 checkpoint、逐样本分数和 transcript 保持原位，本文件不删除或更名任何历史结果。
