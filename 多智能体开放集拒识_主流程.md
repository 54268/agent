# 多智能体开放集拒识主流程

> 本文件是当前项目唯一权威的 Stage 14 主流程入口。Stage 1–13 文档是历史研究记录；Stage 14 的计划、消融、诊断和测试报告统一归档在 `tests/stage14/`。

## 1. 最终框架

最终采用 **A+C 两智能体开放集拒识框架**，不再固定 ABC：

- **Agent A / Identity**：根据身份专家私有输出提出 TOP1/TOP2 已知类假设，能够复查、弃权、查询 C，并发送结构化身份消息；
- **Agent C / Open Set**：根据公开假设和消息，按需调用身份原型、几何原型、OpenMax、冻结边界工具，最后执行“接受当前已知类”或“拒绝为未知”；
- **B Actor 已删除**：B 最有价值的几何原型风险保留为 C 的按需工具；几何 backbone、原型表和风险计算器属于冻结感知工具，不伪装成隐藏智能体；
- **未来未知类细分 Agent**：在 C 完成未知拒识后接入，负责未知簇发现或未知子类分配，不改变当前 A+C 拒识协议。

A 与 C 拥有独立参数、局部观测、动作空间、GRU 状态和消息预算。训练采用集中式 Critic，推理只保留两个 Actor，符合 CTDE（集中训练、分散执行）。

## 2. 为什么仍是真正的多智能体

以下约束均由代码和测试保证：

1. A、C Actor 不共享网络实例和参数；
2. 每个 Agent 只能读取角色允许的局部观测；
3. Ground Truth、样本来源、正式 Unknown 标志不进入 Actor 观测或 Critic 状态；
4. Agent 通过有预算的结构化 Blackboard 消息协作；
5. 本轮查询只能在下一轮得到响应，不允许同轮非因果读写；
6. C 必须显式调用工具并消耗预算，未获得证据时不能直接终局裁决；
7. 打乱 C 工具证据会显著降低 H-score，证明证据真实进入决策；
8. 推理检查点不含集中式 Critic。

## 3. 完整数据流程

### 3.1 外层开放集协议

1. 使用 Stage 7 的 nested Leave-Class-Out 协议创建 outer fold；
2. outer Known 类进入训练、校准和 Known 测试；
3. 训练划分中的留出类只作为 LCO Unknown 代理；
4. 正式 Unknown 只计数，不允许进入训练、校准、工具拟合或当前 smoke 评价；
5. `TrainingEpisodeBank` 对 `formal_unknown=true` 和未批准来源执行 fail-closed 拒绝。

### 3.2 冻结感知母体

从 Stage 5 fold checkpoint 加载并冻结：

- Identity logits、能量、身份原型距离；
- Geometry logits、多视图距离、几何原型距离；
- OpenMax 风险；
- 冻结边界协调器风险。

Stage 13 只用 Known 训练数据生成并认证 Feature-space PUG。Stage 14 不反向修改感知母体。

### 3.3 Episode 构成

训练 episode 只来自：

- outer-fold Known 训练样本；
- train-split LCO Unknown 代理；
- Certified PUG。

每个 episode 保存冻结证据快照，但 Agent 只能通过自己的观测或合法工具动作访问相应字段。

## 4. 单个 Episode 的协作时序

典型 A+C 轨迹：

1. A 读取身份私有观测，执行 `PROPOSE_TOP1` 或 `PROPOSE_TOP2`；
2. C 查询 A，或等待已公开的身份提案；
3. A 可执行 `ABSTAIN`、`RECHECK` 或 `ASK_C`；
4. C 选择一个尚未调用的证据工具，例如 `CHECK_GEO_PROTOTYPE`；
5. 工具结果写入 C 的私有运行状态；若上一轮有人查询 C，则发送有界结构化证据响应；
6. C 根据循环状态、公开消息和已揭示工具证据执行 `ACCEPT_CURRENT_KNOWN` 或 `REJECT_UNKNOWN`；
7. 环境记录动作 mask、动作概率、消息、工具、预算、奖励和终止原因。

动作使用 TOP1/TOP2 排名语义，不把 fold 的类别数量写死进动作维度。

## 5. MAPPO 训练

- Actor：A、C 各一个独立 GRU 策略；
- Critic：一个集中式循环 `V(s)`，只在训练期使用；
- 优化：GAE、PPO policy clipping、value clipping、熵正则、梯度裁剪；
- minibatch：按完整 episode 切分，保留循环状态边界；
- 奖励：正确接受/拒绝为正，错误终局和超时为负，并扣除步数、消息和工具成本；
- 采样：Known 与代理 Unknown 平衡采样。

## 6. 当前结果边界

Oracle outer fold 0、seed 42 的全量单折运行使用 22,400 个 Known 训练样本、5,600 个 LCO 代理、3,200 个校准样本、8,000 个测试样本和 1,283 个 Certified PUG，执行 64 updates × 256 episodes：

| 结果 | Known Acc. | Unknown Recall | H-score | AUROC | OSCR |
|---|---:|---:|---:|---:|---:|
| A+C 全量单折默认策略 | 93.94% | 96.44% | 95.17% | **98.64%** | **98.33%** |
| A+C 在 95% Known 诊断匹配点 | 95.00% | 95.69% | **95.34%** | **98.64%** | **98.33%** |
| 历史 ABC 参考（较小测试集） | 95.31% | 92.19% | 93.72% | 98.61% | 97.80% |

全量 A+C 的 AUROC、OSCR、Unknown Recall 和 H-score均超过历史 ABC 参考。95% Known 匹配点使用测试 Known 标签选择阈值，只用于展示曲线工作点；可部署的校准集阈值结果为 Known Accuracy 94.39%、Unknown Recall 96.25%、H-score 95.31%。

B→C 信息拆解诊断曾给出 98.28%/97.50%的潜力估计；现在无测试标签融合器参与训练的完整 A+C 已实际达到 AUROC 98.64%、OSCR 98.33%，因此不再使用诊断数字代替主结果。

当前结果是单 fold、单 seed 的实现验证，不是论文级最终统计。正式结论需要服务器运行 5 folds × 多随机种子及消融矩阵。

## 7. 运行入口

安装环境：Conda `pytorch`。当前单折训练可在本地 RTX 5070 12 GB 完成，没有显存不足。

训练并评估最终 A+C：

```powershell
conda run --no-capture-output -n pytorch python scripts/experiments/run_stage14_comm_mappo.py --config configs/experiments/stage14_comm_mappo_oracle_ac.json
```

只运行 Stage 14 测试：

```powershell
conda run --no-capture-output -n pytorch python -m pytest -q tests/stage14
```

运行全仓库测试：

```powershell
conda run --no-capture-output -n pytorch python -m pytest -q
```

复现 B→C 诊断：

```powershell
conda run --no-capture-output -n pytorch python tests/stage14/diagnostics/analyze_b_transfer.py
```

## 8. 代码与产物位置

| 内容 | 路径 |
|---|---|
| 最终主配置 | `configs/experiments/stage14_comm_mappo_oracle_ac.json` |
| 真实数据训练入口 | `scripts/experiments/run_stage14_comm_mappo.py` |
| 环境与通信协议 | `src/maros_stage14/env.py` |
| 角色观测 | `src/maros_stage14/observations.py` |
| MAPPO | `src/maros_stage14/mappo.py` |
| 评估与工作点 | `src/maros_stage14/evaluation.py` |
| Actor-only 推理加载 | `src/maros_stage14/runner.py` |
| 最终全量单折结果 | `results/stage14/final_ac_fold0_seed42/` |
| Stage 14 测试总索引 | `tests/stage14/README.md` |
| 历史实验报告 | `tests/stage14/reports/` |
| 测试/消融配置 | `tests/stage14/configs/` |
| 非最终实验产物 | `tests/stage14/artifacts/` |

下次继续开发时，先读本文件，再读 `tests/stage14/README.md`；不需要从 Temp、旧 Stage 文档或多个结果目录反推主流程。
